using UnityEngine;
using UnityEngine.Serialization;

public class Turn : MonoBehaviour
{
    public float turnPoints = 0.0f;
    
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}